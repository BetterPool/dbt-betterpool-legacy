{{ 
    config(
		materialized='table',
        	alias='FACT_ACTIVE_USERS_DAILY',
		tags=["fact"]
    ) 
}}
WITH FINAL AS (
SELECT DISTINCT
    MEMBER_EMAIL AS MEMBER_EMAIL,
    LEAGUE_ID AS LEAGUE_ID,
    PICK_DATE AS ACTIVE_DATE,
    LEAGUE_CATEGORY AS LEAGUE_CATEGORY,
    FISCAL_PERIOD AS FISCAL_PERIOD,
    FISCAL_QUARTER AS FISCAL_QUARTER,
    SOURCE AS SOURCE
FROM {{ ref('fact_active_entries') }}
)

SELECT * FROM FINAL